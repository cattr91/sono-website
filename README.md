sono/
├── index.html              ← clean homepage, no legal markup
├── js/
│   ├── main.js             ← scroll reveal + capability tabs
│   └── legal.js            ← fetches modals, open/close, focus trap, keyboard
└── legal/
    ├── modals.html         ← all modal markup (injected at runtime)
    ├── impressum.html      ← standalone page + direct URL
    ├── datenschutz.html    ← standalone page + direct URL
    ├── terms.html          ← standalone page + direct URL
    └── legal.css           ← shared styles for standalone pages